{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "taskName": "build_makefont",
            "type": "shell",
            "command": "make",
            "args": [
                "-f", "${workspaceRoot}/src/Makefile"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}